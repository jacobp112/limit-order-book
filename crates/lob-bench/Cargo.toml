[package]
name = "lob-bench"
description = "Deterministic workloads, Criterion benchmarks and a latency harness for lob"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
lob = { path = "../lob" }

[dev-dependencies]
criterion = { version = "0.7", default-features = false, features = ["cargo_bench_support"] }

[[bench]]
name = "engine"
harness = false

[lints]
workspace = true
